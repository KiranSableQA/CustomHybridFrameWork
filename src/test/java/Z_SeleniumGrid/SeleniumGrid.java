package Z_SeleniumGrid;

public class SeleniumGrid {
/*
    public static void main(String args[]) throws MalformedURLException, InterruptedException {

        // The URL will be IP Address of Hub Machine + Hub Port + /wd/hub
        // Example: http://192.168.13.1:4444/wd/hub OR http://localhost:4444/wd/hub
        String huburl = "http://localhost:4444/wd/hub";

        DesiredCapabilities cap = new DesiredCapabilities();

        cap.setPlatform(Platform.WIN11); // For Windows 11
        cap.setBrowserName("chrome");    // Can change to "MicrosoftEdge", "firefox", etc.

        WebDriver driver = new RemoteWebDriver(new URL(huburl), cap);

        driver.get("https://www.google.com");
        System.out.println(driver.getTitle());

        driver.quit();
    }
    */
}


	
// http://localhost:4444
//http://172.26.70.118:4444
